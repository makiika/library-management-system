import React,{useEffect} from 'react'
import { useHistory } from 'react-router';

const Home = () => {
  const history = useHistory()

  return (
    <div>
      
    </div>
  )
}

export default Home
